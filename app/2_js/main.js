(function($) {

    $(document).ready(function() {

   //  	setInterval(simpleSlider, 10000);

   //  	function simpleSlider() {

			// 	var curImg = $('.panoramaSlider .current');
			// 	var nextImg = curImg.next('div');
			// 	var activeMenu = $('.panoramaSlider nav .active');
			// 	var nextActiveMenu = activeMenu.next();

			// 	if (nextImg.length == 0) {
			// 		nextImg = $('.panoramaSlider .wideContainer > div:first');
			// 	}

			// 	if (nextActiveMenu.length == 0) {
			// 		nextActiveMenu = $('.panoramaSlider nav li:first');
			// 	}


			// 	nextImg.animate({'right' : '0px'}, 500, function(){
			// 		nextImg.addClass('current').removeAttr('style');
			// 		curImg.removeClass('current');
			// 		nextActiveMenu.addClass('active');
			// 		activeMenu.removeClass('active');
			// 	});

			// }

    });

})(jQuery);